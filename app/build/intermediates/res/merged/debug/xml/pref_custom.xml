<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch_phone"
        android:summary="@string/pref_record_Phone_info"
        android:title="@string/pref_record_Phone" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch_beacons"
        android:summary="@string/pref_record_Beacons_info"
        android:title="@string/pref_record_Beacons" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch_band"
        android:summary="@string/pref_record_Band_info"
        android:title="@string/pref_record_Band" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_record_folderName_model"
        android:inputType="textCapWords"
        android:key="example_text"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:summary="@string/pref_record_folderName_display_info"
        android:title="@string/pref_record_folderName_display_name" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

</PreferenceScreen>
